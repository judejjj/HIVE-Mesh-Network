<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hive_dark"
    android:padding="30dp">

    <TextView
        android:id="@+id/tvConfigTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SYSTEM PROTOCOLS"
        android:textColor="@color/white"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Switch
        android:id="@+id/switchStealth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="STEALTH MODE (DIM SCREEN)"
        android:textColor="@color/hive_text_secondary"
        android:thumbTint="@color/hive_accent"
        android:trackTint="#505050"
        app:layout_constraintTop_toBottomOf="@id/tvConfigTitle" />

    <Switch
        android:id="@+id/switchLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="ACTIVE LOCATION TRACKING"
        android:textColor="@color/hive_text_secondary"
        android:checked="true"
        android:thumbTint="@color/hive_accent"
        android:trackTint="#505050"
        app:layout_constraintTop_toBottomOf="@id/switchStealth" />

    <Button
        android:id="@+id/btnWipe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:text="EMERGENCY WIPE (CLEAR CACHE)"
        android:backgroundTint="#D50000"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/switchLocation" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnLogout"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="20dp"
        android:text="TERMINATE SESSION"
        android:background="@drawable/bg_input_glass"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/btnWipe" />

</androidx.constraintlayout.widget.ConstraintLayout>